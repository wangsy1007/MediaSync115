services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        APP_BUILD_VERSION: "1.0.2"
        APP_BUILD_TAG: "1.0.2"
    image: mediasync115:latest
    container_name: mediasync115
    restart: unless-stopped
    working_dir: /app
    ports:
      - "5173:80"
    volumes:
      - ./backend/data:/app/data
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1/healthz', timeout=10)"
        ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
