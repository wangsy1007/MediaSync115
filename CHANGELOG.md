# Changelog

## 1.3.1

### 重点更新
- 修复探索「更多」页榜单 source 错配导致影视卡片空白（TMDB/豆瓣/猫眼自动推断数据源）。
- 修复探索转存 HDHive 资源命中却无法转存的问题（解锁前先预筛归属/ISO）。
- 转存队列执行期间延迟归档、STRM、订阅扫描及 Emby/飞牛索引同步，避免连续转存被打断。
- 探索转存队列空闲后再触发归档，修复归档扫描卡住后无法继续执行的问题。
- 详情页转存/离线按钮增加点击即时提示。

## 1.3.0

### 重点更新
- 日志页面支持自动刷新。
- ISO 原盘播放对齐 SmartStrm 302 直链链路，强制本地 Range 反代，根治播放卡顿/退出。
- 订阅转存默认跳过 ISO/IMG 原盘资源。
- 强化 STRM 全量生成入口：清空目录后自动升级为全量重建。
- 302 直链播放全面对齐，锁定直链绑定的 UA 一致性。

## 1.2.7

### 重点更新
- 移除猜你想看功能。
- 清理 HDHive 账号密码登录相关提示，默认使用 Cookie 认证。
- 删除 HDHive 签到方式选项，默认 Cookie 签到。
- 修复 115 网盘离线下载配额和任务列表接口方法名不匹配。
- 修复探索页面切换榜单时不必要的加载状态。
- 提升探索榜单每批角标查询上限至 50，修复角标不能全部实时显示。
- 修复订阅转存串台问题：通用标签资源转存前校验 115 分享文件名 + pansou 预过滤。
- 移除 el-message-box/dialog 的 contain 属性，修复弹窗布局错乱。

## 1.2.6

### 重点更新
- 修复订阅自动转存串台：TG 搜索补充片名/年份相关性过滤，入库与转存前校验资源归属（含 tmdbid 标签与 TG 资源标题）。
- 不匹配订阅的资源将被过滤或跳过转存，并记录 `resource_relevance_filtered` / `auto_transfer_item_skipped_mismatch` 日志。

## 1.2.5

### 重点更新
- 修复豆瓣探索页影视卡片入库角标不显示：索引未就绪时不缓存无角标状态，并为豆瓣条目回填 TMDB ID 后再查 Emby/飞牛。
- 探索页卡片移除左上角排名编号，入库角标移至左上角。
- 移除 HDHive API Key 登录方式，保留账号密码登录与 Cookie 兜底。

## 1.2.4

### 重点更新
- 修复 HDHive 已解锁资源无法获取 115 分享链接的问题（从解锁 API 响应提取链接）。
- HDHive 自动解锁改为每次任务最多解锁 1 条符合要求的资源，避免 0 积分资源重复解锁。
- 影视卡片一键转存按「订阅转存-资源获取优先级」瀑布式查找，不再默认合并所有来源后优先 HDHive。
- 画质全局偏好改为排序优先：优先尝试勾选画质，无匹配时仍尝试其他资源。
- 日志中心「清空日志」改为直接清空，不再弹出确认框。

## 1.2.3

### 重点更新
- 修复新浏览器首次访问时长时间卡在首页、迟迟不显示登录页的问题。
- 会话探测改为快速失败，未登录用户立即进入登录页。

## 1.2.2

### 重点更新
- 修复一键转存影视识别错误：严格区分 TMDB/豆瓣 ID，豆瓣条目强制重新匹配。
- 演职员详情与关注动态支持订阅、转存及 Emby/飞牛入库角标；优化演职员作品列表排版。
- HDHive Web 客户端与解锁流程、115 分享目录递归及链接清洗、选集转存体验修复。
- 搜索页移除海报卡片「片单」按钮。

## 1.2.1

### 重点更新
- 新增片单与演职员关注：支持创建片单、补缺订阅、探索页加入片单。
- 片单导入支持流媒体平台（Netflix、Disney+、爱奇艺等）与奖项盛典（奥斯卡、金像奖、华表奖等）预设目录。
- 修复 TMDB 地区为 CN 时流媒体片库预览为 0 的问题（自动回退 HK/TW/US 片库地区）。
- 演职员关注卡片优化头像补全与横向布局；新作仅统计严格晚于今天的上映/首播。

## 1.2.0

### 重点更新
- 归档二级目录自定义配置，支持按分类映射归档子目录。
- 归档自定义命名格式：支持模板与 TMDB ID、分辨率、HDR、编码等画质变量。
- 界面扁平化设计，去除玻璃拟态、渐变与阴影效果。
- 侧栏宽度随 logo 自适应；浏览器标签页图标与侧栏 logo 统一。
- 探索首页改为榜单行级骨架屏；修复侧栏设置导航闪回问题。

## 1.1.39

### 重点更新
- 修复豆瓣及 TMDB 更多榜单页面骨架屏占位符不显示的问题（补充缺失的 --ms-bg-hover CSS 变量）。

## 1.1.38

### 重点更新
- 修复 115 proapi 分享列表使用 `fn`/`fs` 字段时，订阅自动转存报「没有有效的文件ID」的问题。
- 修复订阅立即执行 500 与 share/snap 接口 405 降级逻辑。

## 1.1.37

### 重点更新
- 新增夸克网盘详情 Tab 与设置页配置；界面设置支持夸克主/子标签显示与排序。
- 代理不可达时探索页自动直连；代理检测支持未配置应用代理时的系统/路由器全局网络。
- 优化夸克默认目录选择与目录选择弹窗列表交互。

## 1.1.33

### 重点更新
- 统一订阅自动清理策略：电影在转存成功或 Emby/飞牛已入库时可清理；剧集在订阅范围内不缺集时可清理。
- 「只追新集」模式在缺集为 0 时仍检查待播集，避免连载剧被误删。

## 1.1.32

### 重点更新
- 探索「更多影视」页：首屏仍快速返回榜单，未覆盖条目通过后台分批查询 Emby/飞牛在库状态，在库角标异步补齐；预取时同步合并角标映射。

## 1.1.31

### 重点更新
- 修复 TG Bot 启动时因 Telegram API 超时导致整个应用无法启动的问题；Bot 连接失败时主服务照常运行，可在设置中稍后重启 Bot。

## 1.1.30

### 重点更新
- 订阅自动转存：首个分享链接失效时，自动尝试下一条链接并补充搜索，直至转存成功或无可试资源。
- 刷新页面时采用非阻塞登录校验，移除长时间「正在验证登录状态」全屏等待。
- 修复 TG Bot「保存配置」504 超时；优化探索「更多」页首屏加载与卡片布局稳定性。
- 精简 115 扫码登录可选设备列表。

## 1.1.29

### 重点更新
- 修复订阅检查任务因查询未包含 `douban_id` 导致启动时报错的问题。
- 容器启动时不再立即执行订阅检查与榜单同步，避免阻塞 `/health` 就绪检测。

## 1.1.28

### 重点更新
- 移除探索首页启动预热：减轻容器启动时的外部 API 压力，首页榜单仍按视口懒加载。

## 1.1.27

### 重点更新
- 修复 TG Bot「保存配置」按钮长时间 loading：保存与重启分离，并为 Bot 启停增加超时。
- 运行时设置保存请求延长超时，避免配置写入被误判为无响应。

## 1.1.26

### 重点更新
- 修复探索页搜索后左侧导航无法跳转其它页面的问题（搜索 URL 改走 Vue Router）。
- 优化侧栏菜单导航逻辑，避免探索子菜单与路由冲突。
- 界面设置保存：延长运行时配置超时并改进错误提示与 loading 状态。

## 1.1.25

### 重点更新
- 豆瓣/TMDB「更多影视」探索页：触底预取与展示逻辑优化，滚动加载更顺滑。
- 豆瓣探索分页请求降低同步 TMDB 解析条数，减轻分页接口延迟。

## 1.1.3

### 重点更新
- 影视探索首页海报骨架屏改为按当前容器宽度动态计算数量，避免宽屏下始终只显示固定 6 张占位卡片。

## 1.1.2

### 重点更新
- 探索页首屏改为优先返回豆瓣列表，TMDB 匹配改后台异步回填，降低 NAS 重启后的首屏阻塞。
- Nginx 与前端不再把探索页真实 502 误提示为“后端启动中”，便于定位外部源或代理问题。
- 容器启动日志补充前后端成功提示，并延长健康检查启动宽限时间。

## 1.1.1

### 重点更新
- 新增可选 Kafka 埋点能力，用于记录搜索、订阅、转存等事件。
- 提升订阅定时调度稳定性，补充重启恢复、误触发容错和重复执行保护。
- 优化容器启动阶段体验，前端可更早可用，后端预热期间不再直接暴露原始 502。
- TG Bot 影视通知和交互消息新增海报预览。
- 打通转存后的自动归档和归档完成后的自动 STRM 生成链路。

### 后端
- 新增 `media_postprocess_service`，统一处理转存后的归档和 STRM 自动化流程。
- 修复 STRM 设置保存校验，允许先保存配置，再在真正生成时检查运行前提。
- 增强调度器手动执行能力，支持禁用任务检查和 `force` 解锁卡住任务。
- 将 `misfire_grace_time` 调整为更稳妥的策略，避免服务重启后错过应执行任务。
- 统一订阅后台执行和定时执行的并发保护，减少同频道重复跑任务的风险。

### 前端
- 新增后端启动中的友好提示与自动等待逻辑，降低启动期 502 干扰。
- 同步更新 STRM 页面与启动流程相关的交互体验。

### 媒体流程
- 手动转存、探索页转存、工作流转存、订阅自动转存统一接入：
  `保存到 115 -> 自动归档 -> 自动生成 STRM`
- 离线下载完成后仍按原有监控链路自动触发归档，并在归档完成后继续自动生成 STRM。

### Telegram Bot
- 影视详情、资源列表、转存结果、离线结果、订阅通知均支持海报预览。

### Docker
- `compose.yaml` 增加可选 `KAFKA_BOOTSTRAP_SERVERS` 透传。
- all-in-one Nginx/API 启动流程增加后端预热兜底逻辑，提升启动阶段可用性。
