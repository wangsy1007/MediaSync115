---
description: 代码修改后的本地提交与 Docker 部署流程
alwaysApply: true
---

# 修改代码后的必做流程

每次完成代码修改后，**必须**按顺序执行，不要跳过：

1. **本地 Git 提交**
   - 将所有相关变更 `git add` 并提交到本地仓库
   - 提交信息使用**中文**，格式：`feat:` / `fix:` / `chore:` / `refactor:` 等 + 简要说明
   - **不要** push 到远程，除非用户明确要求
   - **不要** 提交 `.env`、密钥等敏感文件

2. **重新构建并部署 Docker**
   ```bash
   docker compose up -d --build
   ```

3. **确认服务就绪**
   - 检查容器已启动且应用预热完成（必要时查看日志或健康检查）

仅在用户明确说「不要提交」或「不要部署」时可跳过对应步骤。
