---
description: 代码修改后的本地提交与 Docker 部署流程
alwaysApply: true
---

# 修改代码后的必做流程

每次完成代码修改后，**必须**按顺序执行，不要跳过：

1. **本机自动部署最新 Docker 镜像（每次改代码后立即执行，无需用户再提醒）**
   - 在**当前开发机/本机**项目根目录执行，用最新代码重建镜像并拉起容器：
   ```bash
   docker compose up -d --build
   ```
   - 与是否已 Git 提交无关：只要改了代码就要部署，确保本机跑的是最新构建。

2. **本地 Git 提交**（用户要求提交或项目惯例需要时）
   - 将所有相关变更 `git add` 并提交到本地仓库
   - 提交信息使用**中文**，格式：`feat:` / `fix:` / `chore:` / `refactor:` 等 + 简要说明
   - **不要** push 到远程，除非用户明确要求
   - **不要** 提交 `.env`、密钥等敏感文件
   - 若本步执行了提交，提交完成后**再执行一次**上述 Docker 部署（以提交后的代码为准）。

3. **确认服务就绪**
   - 检查容器已启动且应用预热完成（必要时查看日志或健康检查）

仅在用户明确说「不要提交」或「不要部署」时可跳过对应步骤。
